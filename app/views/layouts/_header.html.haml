.nav
  %a.nav__title{:href => "/"} Weebcomic
  - if user_signed_in?
    %ul.nav__menu
      %li.ls-btn
        = link_to "ログアウト", destroy_user_session_path, method: :delete
      %li.ls-btn
        %a{:href => "/users/#{current_user.id}"} マイページ
  - else
    %ul.nav__menu
      %li.ls-btn= link_to "新規登録", new_user_registration_path
      %li.ls-btn= link_to "ログイン", new_user_session_path
